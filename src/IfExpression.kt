fun main() {
   val isAdult = true;
   val myExpression = if (isAdult) {
       "Yes, you can enter"
   } else {
       "Sorry, you aren't allowed to be here"
   }

   val isNullable: Boolean? = null;

   println(myExpression)
    
}