fun main() {
  /* Ways to convert one datatype to another one
   * in this case converting number to string and a string to number
  */
  val number = "10";
  println(number + 10.toString()); // 1010
  val number2 = 10
  println(number.toInt() + number2); // 20
}