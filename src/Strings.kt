fun main() {
   val name = "John"
   println("Hello $name!")
   println(name.uppercase())
   println(name.lowercase())
   println(name.length)
   println(name[0]) // J
   println(name[name.length - 1])  // n
}